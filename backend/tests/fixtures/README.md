# Test fixtures directory for OCR tests

This directory contains test images for OCR service testing.

Images are small placeholders to test the OCR service's ability to handle
image data without requiring actual book scans.
